# v0.0.30 版本
该版本为组件库的0.0.30版本。在这个版本中，完成了布局与滚动组件的实现。并采用 `Vitest` 完成单元测试。该组件在 Vue 文件中实现，到此已经发布了30次，为早期的测试版本。

## 当前组件库组件

### 基础组件
- Button
- Border
- a
- text

### 布局与滚动组件
- scroll（全局）
- scrollbar(局部)
- container
- layout