import{_ as t,c as o,o as d,ah as a}from"./chunks/framework.D3tsReks.js";const u=JSON.parse('{"title":"useOverallComputed","description":"","frontmatter":{},"headers":[],"relativePath":"design/hooks/useOverall.md","filePath":"design/hooks/useOverall.md"}'),r={name:"design/hooks/useOverall.md"};function s(l,e,n,i,c,h){return d(),o("div",null,[...e[0]||(e[0]=[a('<h1 id="useoverallcomputed" tabindex="-1">useOverallComputed <a class="header-anchor" href="#useoverallcomputed" aria-label="Permalink to “useOverallComputed”">​</a></h1><h3 id="useoverallcomputed-布局与容器整体计算-hooks" tabindex="-1">useOverallComputed：布局与容器整体计算 Hooks <a class="header-anchor" href="#useoverallcomputed-布局与容器整体计算-hooks" aria-label="Permalink to “useOverallComputed：布局与容器整体计算 Hooks”">​</a></h3><h2 id="痛点来源" tabindex="-1">痛点来源 <a class="header-anchor" href="#痛点来源" aria-label="Permalink to “痛点来源”">​</a></h2><p>布局系统（Layout / Row / Col）和容器系统（Container / Header / Main / Aside / Footer）是组件库中最基础的结构组件，却常常伴随着大量重复的类名与样式计算逻辑：</p><ul><li>需要根据 <code>gutter</code>、<code>justify</code>、<code>span</code>、<code>offset</code>、<code>push/pull</code> 等 props 生成大量修饰类</li><li>容器需支持 <code>fixed</code>、<code>position</code> 等布局属性，同时处理 header/footer 的固定定位</li><li>栅格系统需要将数字 props 精确转换为 CSS 变量（如 <code>calc(span / 24 * 100%)</code>）</li><li>宽度、高度、背景等尺寸样式需统一单位处理并注入 CSS 变量</li><li>每个子区域（如 header、main）都需要独立的类名与潜在的状态类</li></ul><p>若在每个布局组件中单独实现这些逻辑，会导致代码高度冗余、命名不统一、样式变量难以维护。</p><h2 id="设计目标" tabindex="-1">设计目标 <a class="header-anchor" href="#设计目标" aria-label="Permalink to “设计目标”">​</a></h2><p>本文件集中封装了布局与容器组件的<strong>整体类名与样式计算逻辑</strong>，通过复用已有的 <code>useClassComputed</code> 和 <code>useStyleComputed</code>，提供高度一致、可配置的计算方案，让 Layout 和 Container 系列组件的实现极简且规范。</p><h2 id="核心-hooks-组成" tabindex="-1">核心 Hooks 组成 <a class="header-anchor" href="#核心-hooks-组成" aria-label="Permalink to “核心 Hooks 组成”">​</a></h2><table tabindex="0"><thead><tr><th>Hook</th><th>职责</th><th>适用组件</th></tr></thead><tbody><tr><td><strong>useContainerOverAllComputed</strong></td><td>计算 Container 及其各区域（header/main/aside/footer）的类名</td><td>Container、Header、Main 等</td></tr><tr><td><strong>useContainerStyle</strong></td><td>计算 Container 的尺寸、背景等内联样式与 CSS 变量</td><td>Container</td></tr><tr><td><strong>useLayoutOverAllComputed</strong></td><td>同时计算 Row 和 Col 的类名与样式（包括栅格变量计算）</td><td>Row、Col（常组合使用）</td></tr></tbody></table><h2 id="核心特性" tabindex="-1">核心特性 <a class="header-anchor" href="#核心特性" aria-label="Permalink to “核心特性”">​</a></h2><table tabindex="0"><thead><tr><th>特性</th><th>说明</th></tr></thead><tbody><tr><td><strong>深度复用通用 Hooks</strong></td><td>全面基于 <code>useClassComputed</code> 和 <code>useStyleComputed</code>，保持逻辑一致性</td></tr><tr><td><strong>精准类名生成</strong></td><td>支持 BEM 规范的 <code>--modifier</code> 格式，自动处理 flag 与 prop 值类</td></tr><tr><td><strong>智能 CSS 变量注入</strong></td><td>自动处理单位（数字 → px）、calc 计算（栅格百分比、偏移、推拉）</td></tr><tr><td><strong>栅格系统完整支持</strong></td><td>精确实现 24 栅格的 <code>span</code>、<code>offset</code>、<code>pull</code>、<code>push</code> 等所有属性</td></tr><tr><td><strong>容器布局灵活性</strong></td><td>支持 <code>fixed</code> 固定、右侧布局（<code>position-right</code>）等常见需求</td></tr><tr><td><strong>类型安全组合</strong></td><td>通过接口合并（<code>CombinedProps</code>）实现 Row 与 Col props 的统一处理</td></tr><tr><td><strong>零冗余设计</strong></td><td>所有重复逻辑集中于此，组件只需调用并绑定返回值</td></tr></tbody></table><h2 id="设计价值" tabindex="-1">设计价值 <a class="header-anchor" href="#设计价值" aria-label="Permalink to “设计价值”">​</a></h2><p>这些 Hooks 将布局系统中最为繁琐的类名爆炸与样式变量计算彻底集中管理，确保：</p><p>所有布局组件视觉与行为高度统一 栅格计算精准、无偏差 CSS 变量规范注入，便于主题覆盖与动态调整 组件代码极度精简（仅负责结构与插槽） 易于未来扩展（如响应式断点、更多 justify 模式）</p><p>它们与 useClassComputed、useStyleComputed 形成完整链路，完美体现了逻辑分层、复用至上的设计哲学：基础结构组件不应被琐碎的类名与样式逻辑拖累，而应专注于“布局的语义表达”。 用最统一的计算，支撑最灵活的布局系统。</p>',16)])])}const m=t(r,[["render",s]]);export{u as __pageData,m as default};
